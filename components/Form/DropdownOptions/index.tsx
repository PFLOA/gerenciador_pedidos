import style from './style.module.scss';

interface DropdownOptionsProps {
	id: string;
	value: string;
	name: string;
	setData(valor: any): void;
}

const DropdownOptions: React.FC<DropdownOptionsProps> = ({ id, value, name, setData }) => {

	

	return (
		
	);
};

export default DropdownOptions;
